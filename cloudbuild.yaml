steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'us-central1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/careerpilot-ai:latest', '.']
    timeout: '1200s'
options:
  machineType: 'E2_HIGHCPU_8'
timeout: '1800s'
images:
  - 'us-central1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/careerpilot-ai:latest'
